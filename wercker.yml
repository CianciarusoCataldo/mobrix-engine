box: node
build:
  steps:
    - script:
        name: MoBrix-engine lib build
        code: |
          npm i
          npm run build
test:
  steps:
    - script:
        name: MoBrix-engine lib tests
        code: |
          npm i
          npm run test